# MEMORIZE.md — 对话记录（防上下文断开）

记录本仓库里用户提问原文，以及实现者当时的回答要点与落地结果。  
角色：实现者，不是交易员。不改 `AGENTS.md` / `RULES.md` 仓位数字、否决项、主路径。

对话日期：2026-08-30。  
工作区：`D:\jiaoyi\my-trading-desk`。

---

## 提问 0

> 列出本目录的文件

### 回答

当时根目录：

- `AGENTS.md` 工作章程
- `PROFILE.md` 交易者画像
- `RULES.md` 交易规则
- `README.md` 项目说明
- `data/` 数据目录（当时为空）
- `journal/TEMPLATE.md` 复盘模板

---

## 提问 1

> 做本地个人交易台网站。读 AGENTS.md、PROFILE.md、RULES.md。  
> 你是实现者，不是交易员。
>
> 硬禁止：
> - 改 RULES 仓位数字、否决项、主路径
> - 发明新指标（只许 MACD(7,28,4) + KDJ + 均线辅助展示）
> - 触发文案必须写：这是事实记录
>
> 目标界面（对照兔窝）：
> 侧栏：首页、规则扫描、我的观察、我的交易、我的复盘、我的规则、数据与设置  
> 顶栏：GeorgeChin Personal Trade· 本地个人交易空间；「本地数据已连接」；「+ 记录我的想法」
>
> 首页：
> - 「你设置的 N 个观察条件已触发」卡片（代码+名称、条件原文、触发快照；当前行情不改写已确认收盘事实；按钮：查看日线与事实 / 记录我的判断）
> - 待看观察 / 待确认判断 / 监测中心 三个队列数字
> - 「今天规则扫描」摘要：符合 / 继续跟踪 / 观察 / 排除 的只数
> - 每条输出状态只能是：排除、观察、等待、试仓、标准仓、禁止
>
> 规则扫描页：按 RULES 把股票分类，每只写命中哪条、还缺哪条。阈值空缺不得升到试仓/标准仓。按总闸分类
>
> 我的观察：人工添加代码 + 观察条件（可先内置「收盘站上5日均线」「MACD绿柱缩短不创新低」）。  
> 我的交易：手工记账（代码、方向、仓位%、原因），不做券商同步。  
> 我的复盘：按 journal 模板写当天环境/做了什么/没做什么。  
> 我的规则：只读展示 RULES.md / PROFILE.md，网站上不能编辑仓位上限。  
> 数据与设置：上传或放置日线 CSV；可预留 Tushare 配置，没有 token 时用 CSV。
>
> 技术：前端 Vue3+Vite，后端 FastAPI。按 RULES 池子选基础股。README 写安装、放 CSV、启动、打开哪个地址。
>
> 允许改：前端、后端、data/、README、requirements 或 package.json  
> 禁止改：AGENTS.md、RULES.md 只许读不许改数字
>
> 验收：
> 1. 本地能打开首页，侧栏 7 项都能进
> 2. 画交易图
> 3. 满足观察条件会出现首页触发卡片
> 4. 全站搜不到下单、券商、买入指令
> 5. 给出启动命令和截图级说明（每页干什么）

### 回答 / 落地

搭了 FastAPI + Vue3 本地交易台。规则引擎按 `RULES.md` 漏斗走，公开状态只用六个词。`RULES` 没有试仓/标准仓数字，也没有「DIF 靠近」的量化阈值 → **扫描最高停在等待**，不得升到试仓/标准仓。

第一版用仓库内示例 CSV 做界面验收（后来提问 2、3 已改成真实行情，示例不再当数据源）。

启动（现在仍适用）：

```powershell
cd D:\jiaoyi\my-trading-desk
python -m pip install -r backend\requirements.txt
cd frontend
npm install
npm run build
cd ..
python run.py
```

打开 **http://127.0.0.1:8000**。开发热更新：`python run.py` + `cd frontend ; npm run dev`（5173 被占用会改 5174）。

侧栏七项：首页 / 规则扫描 / 我的观察 / 我的交易 / 我的复盘 / 我的规则 / 数据与设置。  
图：K 线 + MA5/10/20 + MACD(7,28,4) + KDJ。  
观察内置两条：日线收盘重新站上5日均线；MACD绿柱缩短不创新低。触发文案固定 **这是事实记录**。  
前端+后端源码不含「下单」「券商」「买入指令」。

---

## 提问 2

> 接下来：1、目前是本地csv数据，我要连接真实数据；2、我的股池要按RULES去筛选出来所有；3

（第 3 点原文被截断，当时只做了 1 和 2，并请用户补第 3 点。后续提问 3 接上了数据源与定时，仍未单独补那条「3」。）

### 回答 / 落地

设置里虽有 Tushare token，但 **没有接口权限**（`trade_cal` / `stock_basic` / `daily` / `daily_basic` 均被拒）。因此没有走 Tushare。

实际取数：

- 全市场快照：新浪 `hs_a`
- 日线：腾讯 K 线（不复权确认收盘）
- 确认收盘日：**2026-08-28**

股池按 RULES §3 硬门槛全市场筛，不截成 13 只示例，也不按 PROFILE「同时跟踪 100 只」截断扫描。100 只只是人的跟踪带宽说明。

当时漏斗（数字会随下一趟同步变）：

| 漏斗 | 只数 |
|---|---|
| 上市 A 股 | 5550 |
| 非 ST | 5345 |
| 市值过 300 亿 | 574 |
| 成交额过 5 亿 | 905 |
| **同时满足入池** | **378** |
| 其中优先样本（沪深300 / 上证50） | 159 |
| 沪股通标签 | 0（该名单当时没取到，不挡入池） |

扫描全量 378：观察 302、等待 74、排除 1、禁止 1。试仓 / 标准仓 = 0。  
日线写入 377，失败 1。

优先样本只打标签：沪股通 / 沪深300 / 上证50；其他过池股也保留（RULES 原文「其他优质股也可以」）。

---

## 提问 3

> 1、帮我数据源选几个后备，不要本地数据。比如腾讯/新浪/东财取数；2、数据更新，什么时候好，然后弄一个定时服务；

### 回答 / 落地

**数据源：** 不再用示例 CSV 当数据源。`data/csv` 只是确认收盘缓存。

日线链：**腾讯 → 新浪 → 东财**（一个断了换下一个）。筛池优先新浪快照。Tushare 仅在 token 真有权限时才试。

探测（提问当日）：腾讯通、新浪通、东财超时、新浪快照通。

顶栏改为：**真实行情已连接 · 腾讯/新浪/东财 确认收盘 YYYY-MM-DD**。

**何时更新：** A 股 15:00 收盘。日线大约 15:10–15:30 才齐。盘中不是确认收盘，**盘中不改写**。

| 北京时间 | 做什么 |
|---|---|
| 交易日 **15:40** | 第一趟：筛池 + 拉确认日线 |
| 交易日 **16:30** | 第二趟：只补失败或缺根 |
| 周六日 | 不跑 |

两套定时：

1. `python run.py` 开着：进程内定时器（数据与设置可关）。
2. Windows 计划任务（电脑开着、交易台没开也跑）：
   - `GeorgeChin-Trade-1540`
   - `GeorgeChin-Trade-1630`
   - 安装脚本：`powershell -ExecutionPolicy Bypass -File D:\jiaoyi\my-trading-desk\scripts\install_schedule.ps1`

手工立刻更新：数据与设置 →「现在更新确认收盘」，或 `D:\jiaoyi\my-trading-desk\sync_once.cmd`。

下次触发（记录时为周日）：**2026-08-31 15:40**。

---

## 提问 4（本条）

> 为防止上下文断了，把我对你的提问和对应回答全记录在IMPLEMENTATION.md

### 回答

写本文件。下文是接上下文时必须先读的硬约束和现状，避免下一次重新发明。

---

## 接上下文必读

### 人

- 市场：A 股。主路径：波段持有（数日到数周）。
- 不做：打板、连板、高位接力、隔夜情绪票。
- 带宽：同时跟踪 100 只（扫描不按 100 截断）。

### 规则引擎（只读 RULES，不改数字）

- 指标：`MACD(7,28,4)` + `KDJ`；均线只辅助展示。
- 池子同时满足才可观察：流通市值 ≥ 300 亿；日成交额 ≥ 5 亿；非 ST / *ST；股价 ≥ 5 元。优先样本：沪股通 / 沪深300 / 上证50；其他过池也可以。
- 1–5 亿成交额不进池，一律排除。
- 规则扫描总闸：排除 → 观察 → 等待 → 买入 → 减仓 / 清仓。买入 = 路径到达，不是成交指令。
- 零轴下/刚过零轴、近20日低位已进扫描器。改 RULES.md **不会自动改 Python**；只有 `rules_bind.py` 里登记过的开关会随文档开关。
- 触发文案必须：**这是事实记录**。当前行情不改写已确认收盘快照。
- 禁止自动成交、禁止「现在立刻买入」口吻。全站不要出现：下单、券商、买入指令。
- 数据不足写「证据不足」，不编 K 线。

### 栈与入口

- 后端：`backend/` FastAPI，`python run.py` → http://127.0.0.1:8000
- 前端：`frontend/` Vue3 + Vite
- 实现者可改：前端、后端、`data/`、README、依赖。不可改：`AGENTS.md`、`RULES.md` 数字。

### 关键代码

- 筛池 + 同步：`backend/app/engine/live.py`
- 腾讯/新浪/东财：`backend/app/engine/eastmoney.py`
- 收盘时钟：`backend/app/engine/clock.py`（15:40 / 16:30 Asia/Shanghai）
- 进程内定时：`backend/app/engine/scheduler.py`
- 规则扫描：`backend/app/engine/scanner.py`
- RULES 开关绑定：`backend/app/engine/rules_bind.py`（只认已实现的句子）
- 一次性同步：`python -m app.jobs` 或根目录 `sync_once.cmd`

### 未完成 / 待用户补

- 提问 2 的「3」原文被截断，从未单独实现。若用户补上第 3 点，从这里继续，不要当没说过。
- 沪股通成分名单曾取到 0，不挡入池，只是优先标签缺这一项。
- Tushare token 无接口权限；积分够了会自动优先试 Tushare。
- 东财日线探测曾超时，链上腾讯、新浪可用即可。
- 网站上不能编辑仓位上限；改规则只改 `RULES.md` 并在 journal 写为什么改。
- **不能指望只改 RULES.md 就更新全部筛选代码。** 扫描器只执行已写成 Python 的条件。`rules_bind.py` 目前只会开关：低位、DIF靠近最低、零轴金叉、股价低位区、近一两日金叉。新逻辑（例如板块相对大盘）必须改 `scanner.py`。
- 板块近 3 日相对大盘：RULES 已写，扫描器尚未实现，不挡筛选。

---

## 提问：增量文件 + 是否只改 RULES.md 就能更新代码

> 给我解释这些增量的文件，我后续只需要该RULES.md就能扫描我的规则，然后更新代码吗？因我我后续可能需要你重新学习

### 回答（结论）

不能。RULES.md 是说明书。扫描跑的是 `scanner.py`。  
已实现的开关：改 RULES 里对应措辞 → 刷新扫描页即可开/关。  
新句子、新条件：必须改代码。下一轮对话先读 `MEMORIZE.md`、`RULES.md`、`scanner.py`、`rules_bind.py`。
