# 交易规则
指标固定：`MACD(7,28,4)` + `KDJ(9,3,3)`。
DIF 即 DIFF，两者同一条线。
数据不足或参数对不上 → `排除`。

本规则只做 **低位金叉波段**。金叉 ≠ 底背离：
- 金叉：收盘 DIF 上穿 DEA
- 底背离：价格创新低，同时 DIF/柱不创新低
底背离暂不作硬闸，避免和金叉混用。启动只认低位金叉。

参考样本（只校准低位，不另开策略）：招商银行 600036，2026-07-02 附近 `MACD(7,28,4)` 零下低位金叉后的 V 型反弹。
## 1. 总闸
每只票每日只输出一个状态，禁止跳步：
`排除` → `观察` → `买入` → `卖出`
上一步条件不齐，停在当前态。
`买入` 只表示路径到达（试仓条件齐），不是成交指令。当日全市场新开 ≤ 1 只试仓。禁止一次输出成批「买入」。
判定用 **已收盘日线**。盘中两线粘合、差 0.01、分时翻红，一律不算金叉。

## 2. 漏斗
按序执行，一票否决即停：
1. 大盘：多 / 空 / 震荡（只定性；未设置只提醒，不挡个股筛选）
2. 板块：只看强度与持续性，不看故事。个股所属板块近 3 个交易日相对大盘不弱（抗跌或领涨）；板块明显弱于大盘 → 不得进入 `买入`
3. 个股：先过池子与否决，再看波段

## 3. 池子
同时满足才可进入 `观察`，否则 `排除`：
- 流通市值 ≥ 300 亿
- 日成交额 ≥ 5 亿
- 非 ST、非 *ST
- 动态市盈 > 0（亏损票排除）
- 股价 ≥ 5 元
- 优先样本：沪股通 / 沪深300 / 上证50；其他过池股也保留
## 4. 否决
命中任一条 → `排除`，不得新开：
- 小盘题材、连板妖股、游资票
- 亏损暴雷股；动态市盈 ≤ 0
- 日成交额 < 1 亿（池子门槛是 ≥ 5 亿；1–5 亿不进池）
- 打板、连板、高位接力、隔夜情绪票
- **J ≥ 80 或 K > 50**（超买区金叉，不得新开）
- **收盘价距近 60 日最高价回撤 ≥ 15%，且 DIF 仍在零轴下**（超涨后的回撤金叉，不得当启动）
- **收盘 < MA30 且 MA30 向下**（空头里的金叉，不是低位启动）

## 5. 观察（未齐则仍观察）
同时满足才继续核买入条件，否则留在 `观察`：
1. MACD 绿柱缩短且不创新低。近一两日即可；刚由绿转红也算。此时仍不要求当天已经金叉。
2. KDJ：K/D 靠近或低于 20，或 KDJ 不创新低；同时必须 **J < 80 且 K ≤ 50**
3. 位置在低位：DIF 靠近近 20 日 DIF 最小值；股价靠近近 20 日最低价一带。中高位才开始缩短绿柱 → 留在 `观察`，不得买入

「两线粘合、绿柱将没」只够继续观察，不够 `买入`。

## 6. 观察 → 买入（试仓）
必须 **上一交易日已收盘** 同时满足，盘中不算：
1. `MACD(7,28,4)`：收盘 DIF > DEA，且前一日 DIF ≤ DEA（当日完成金叉）。不得用「快要上穿」代替。
2. 绿柱已在近多日低点附近连续缩短、不创新低，并向 0 收敛；或收盘已由绿转红。
3. 低位约束（禁止高位金叉追涨），三条都要：
   - DIF 靠近近 20 日 DIF 最小值
   - 金叉发生在零轴下方，或 DIF/DEA 刚过零轴附近；DIF、DEA 已远离零轴上方 → 不得买入
   - 股价仍在近 20 日低位区，不是远离底部后的第二段加速金叉
4. 第 4 节否决全部未命中
5. 当日全账户新开 ≤ 1 只；仓位按试仓（总资金约 10%–15%），不得把观察池一次打满
未齐则留在 `观察`。不得买入。
## 7. 买入 → 卖出
### 7.1 失败离场（优先于波段卖出）
收盘命中任一条 → 当日卖出，退回 `观察`：
- 收盘跌破 **买入日最低价**（若买入日无完整金叉阳线，用买入当日 K 线最低价）
- 收盘跌破 **金叉阳线最低价**
- 金叉后 5 个交易日内红柱没有放大，或再次绿柱拉长

### 7.2 波段卖出
同时满足：
1. 本波红柱长度 < 前一波高点红柱，且 `DIF < REF(DIF,1)`  
   （本波 = 本次由绿转红之后的这一截红柱；不要拿更早另一轮行情的红柱来比）
2. 股价创近 20 日新高：`H = HHV(H,20)`
3. KDJ：K/D 靠近或高于 80 死叉，或 KDJ 不创新高
## 8. 输出与仓位
- 扫描列表只出四个状态：`排除` / `观察` / `买入` / `卖出`
- `买入` 表示路径到达，不是成交指令
- 每日新开 ≤ 1 只
- 单日盯盘 3–5 只即可，从 `观察` / `买入` 里盯，不从已否决名单里找替补
- 已持仓不加仓，除非重新走完第6条且当日尚无新开

## 9. 暂缓
下列两项暂不纳入判定（招行样本里价格后来站上 5/10 日线、量能温和放大，只作事后对照）：
- 近日回撤的精细分档（60 日回撤 ≥15% 已在第4条作闸）
- 成交量是否站上 10 日 / 20 日均线（只作事后对照，不作闸门）